#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: program that print a text using puts
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts("/"Programming is like building a multilingual puzzle);
return (0);
}
