#include <stdio.h>
/**
 *
 * main - Entry point
 *
 * printing a phrase "Programming is like building a multilingual puzzle"
 *
 * return - always zero (0)
 */
int main(void)
/**
 * function main - its a function that will print the phrase
 */
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
