#include "main.h"
/**
 * add - function that adds two integers.
 * @a: collect first integer
 * @b: collect second integer
 * Return: returns the result.
 */
int add(int a, int b)
{
	return (a + b);
}
